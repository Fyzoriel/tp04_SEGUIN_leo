import { DataControlDirective } from './data-control.directive';

describe('DataControlDirective', () => {
  it('should create an instance', () => {
    const directive = new DataControlDirective();
    expect(directive).toBeTruthy();
  });
});
